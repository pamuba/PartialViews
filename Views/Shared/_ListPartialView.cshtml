@model ListModel

<div class="list-container">
    <h2>@Model.ListTitle</h2>
    <ul class="list">
        @foreach(string item in Model.ListItems)
        {
            <li>@item</li>    
        }
    </ul>
</div>